# Deferred launch work

| # | Item | Notes |
|---|------|--------|
| 1 | **WhatsApp live** | Soft-skips today; Meta templates + `WHATSAPP_*` tokens |

## Recently completed
- Refunds via Razorpay (`processAdminRefund` + webhook idempotency)
- Booking invoice PDF download + payment-received email attachment
- Coupons: checkout preview + `/admin/coupons` CRUD
- Admin RBAC nav gating + `/admin/roles` assign UI
- Docs: permission matrix + R2/NextAuth drift on checklist

Also still useful soon: contact form → admin Messages polish, brand placeholders, live Razorpay, Upstash.
